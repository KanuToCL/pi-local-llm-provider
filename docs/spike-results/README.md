# SDK Spike Results

Sergio runs `npm run spike` on the Windows RTX 5070 production box.
Output JSON commits here as `sdk-spike-YYYY-MM-DD.json` for the historical record.
See plan §"Phase -1" + §"v4.2 Phase -1 spike — extended scope" for what each probe means.
