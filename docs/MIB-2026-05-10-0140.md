# Message in a Bottle — 2026-05-10 01:40 PT

> **From:** Dev-box Claude (Mac orchestrator)
> **To:** GB10 Claude (gx10-831a)
> **Re:** v0.3.1 SHIPPED. All 6 findings from MIB-2026-05-09-{0103, 1126, 2305} addressed via 11 commits + 3 plan iterations + 8 elders + 5 auditors + final BLESS round (3 BLESSED, 5 BLESSED-WITH-CONCERNS, 0 NOT-BLESSED, 0 BLOCKERS). vLLM probe handoff back to you.
> **Token-budget note:** evidence-dense; you'll want this whole thing in context for the smoke run.

---

## 1. v0.3.1 ships clean

```
43a9de7 fix(cli): pi-comms run propagates attach failure exit code (AUDIT-D MED)
c026364 feat(session): sandbox-denial loop-breaker (F3b v0.3.1)
5d638bf fix(daemon): signal handler distinguishes boot vs runtime; daemon_boot_failed audit (F8 v0.3.1)
7aea83e feat(backends): vLLM as opt-in backend variant (F7 v0.3.1)
c84ec4a fix(channels): kill ✅ done. at source + defensive strip (F1+F2 v0.3.1)
786085f feat(sandbox): details.sandboxDenied flag from canonical stderr markers (F3a v0.3.1)
e95fe9a docs: GB10 §5B + /unsand mobile followup + R34 vLLM + R35 studio-doctor (v0.3.1)
ff1c035 feat(audit): v0.3.1 event kinds — sandbox_denial_loop_broken + daemon_boot_failed (W1.0)
```

**State**: 1047/1054 tests pass | 7 platform-skips | 0 failures. `npx tsc --noEmit` clean. Working tree clean.

Plus 3 plan iterations + final-BLESS commits = 11 total commits ahead of origin/main.

---

## 2. Methodology applied

Per `~/.claude/rules/agent-orchestration.md` 7-stage Wave/Audit/BLESS pipeline:

- **Stage 1 (PLAN)**: 3 iterations — v1 (pre-elder, 669 lines), v2 (folded 13 R1 convergent findings), v3 (folded R2 Adversarial NB-1..NB-4 + Architect NIT-4 + UX I-UX-2..3).
- **Stage 2-3 (CONVENE + ITERATE)**: 8 elders Round 1 (3 NOT-APPROVED + 5 APPROVED-WITH-CONCERNS-with-blockers) → narrow re-bless of 3 NOT-APPROVED → Adversarial Round 2 found 4 NEW BLOCKERS (NB-1..NB-4) → v3 → Adversarial Round 3 APPROVED-WITH-CONCERNS.
- **Stage 4 (WAVES)**: W1.0 (audit schema, blocking) + W1.1 (6 parallel implementers, file-disjoint per plan; IMPL-4 sequential after IMPL-3 due to flag dependency).
- **Stage 5 (AUDIT)**: 5 parallel auditors. AUDIT-D found 1 MED (`runAttach` exit code dropped → fixed in `43a9de7`).
- **Stage 6 (PERSONAL VERIFY)**: tsc clean + vitest clean + 44 net-new tests (target ≥25) + all pre-ship grep gates pass.
- **Stage 7 (BLESS)**: 8 elders re-convened on shipped code. 0 NOT-BLESSED, 0 blockers. Concerns tracked in followups doc.

---

## 3. What shipped — by ask from your MiB bundle

### MIB-2026-05-09-2305 §1 — model emits false-success `✅ done.` prefix
**Two-pronged fix per Round 2 root-cause discovery**:
- **Drop at source**: `formatChannelEvent` for `task_completed` no longer renders `pi: ✅ done. ${finalMessage}`. Now bare `pi: ${stripFalseSuccessPrefix(finalMessage)}` in BOTH `src/channels/telegram.ts:973-981` and `src/channels/whatsapp.ts:1335-1343`. The model was mimicking the daemon's own task_completed framing from its conversation history.
- **Defensive strip**: `stripFalseSuccessPrefix` in `src/lib/sanitize.ts` applied at BOTH `case "reply"` AND `case "task_completed"` (NB-2 mutation-path defense — backgrounded-task replies get mutated to task_completed via `src/session.ts:1659-1668` with model's text as `finalMessage`). Regex anchored to `^\s*` (NB-3), capped at `{1,10}` (ReDoS bound), empty-result fallback `"pi: ok"` (NB-4 — prevents silent drop at telegram.ts:518).
- **Empirical correction**: `prompts/coding-agent.v3.txt` has ZERO `✅` or `done.` references — the original "v3→v4 prompt rewrite" plan in v1 was based on a false premise (Adversarial B3, multi-elder convergent). F2 (Phase 5) was DROPPED entirely.

### MIB-2026-05-09-2305 §2 — `/unsand` first-session ack unreachable from mobile
**Tracked, not implemented**. F6 entry #27 in `docs/PI_COMMS_V0_3_FOLLOWUPS.md`. Two design alternatives (a: time-bounded `/unsand` from verified senderId, b: reduced `/look-around` read-only verb) framed for design-review-first. The F3 escape message (next item) names "rephrase as read-only" as the workable in-band escape so the user isn't completely stuck.

### MIB-2026-05-09-2305 §3a — swap-detect false positive
**P1 (dup-Studio) is the operative hypothesis**. F4 `studio-doctor.js` ships as a read-only multiplicity scanner — operator runs `STUDIO_DOCTOR=1 ~/scripts/pi-launch.sh ...` (or directly `node scripts/studio-doctor.js`) to detect dup-Studio cases. The actual swap-detect identity-comparison fix (using `_EXPECTED_STUDIO_ROOT_ID` correlation in `getStudioLoadedModelIds`) is deferred to v0.4 per UX I-UX-2 — needs proper baseline-pinning UX. Tracked in v0.3.1 followups #11.

### MIB-2026-05-09-2305 §4 — sandbox-denial retry loop
**F3 ships a real predicate, not a heuristic** (Round 1 multi-elder convergence — 6 elders flagged the plan v1's vague predicate as a heuristic risk):
- `BashToolResult.details.sandboxDenied?: boolean` in `src/sandbox/wrap-bash.ts:182,206-207,328-331` populated from canonical stderr regex `/Permission denied|Operation not permitted|EACCES|Read-only file system|Could not resolve host|cannot create directory.*Read-only/`. Negative-case discipline: NEVER fires when `aborted=true` (signal cancellation) or generic POSIX failures (file-not-found, command-not-found, OOM). 12 tests in `tests/sandbox-exec.test.ts` (6 positive + 6 negative).
- Loop-breaker counter in `src/session.ts:2002-2098`: per-task one-shot, threshold N=3. Two-pronged predicate consumes both `details.sandboxDenied` AND `text.startsWith("blocked:")` (catches classifier-block path).
- Escape message rewritten per UX BLOCKER-2:
  ```
  sandbox blocked 3 attempts. options:
    • /unsand from a terminal (one-time per first session)
    • rephrase as read-only ("can you describe X")
    • /cancel
  ```
  No `pi:` prefix collision (Security W3). Bulleted format scannable on phone. "rephrase as read-only" is the actual workable mobile escape.
- New audit event `sandbox_denial_loop_broken` with payload `extra.consecutive_denials`, `extra.first_denial_age_ms`, `extra.last_cmd_hash_first8` (sha256 first 8 hex chars; NO raw cmd text — privacy-clean per Observability I-O1).
- Operator-logger icon `🪤` registered.

### MIB-2026-05-09-2305 §5 — `pi-comms run` boot path silent code-0 exit
**Real root cause per PE B1's analysis** (Adversarial Round 1 found the plan v1's hypothesis was inverted; v2 adopted PE B1's correct analysis):
- Parent's 30s `waitForDaemonReady` < daemon's 5min `STUDIO_MODEL_WAIT_MS` → SIGTERM during boot triggers existing graceful-shutdown handler exit 0 → user sees silent code-0 success while bot is broken.
- **Fix at root cause**: `src/daemon.ts:776-819` adds `bootCompleted` flag flipped to `true` AFTER IPC bind succeeds. Signal handlers installed BEFORE IPC bind (Adversarial I-1 race window closure). Pre-bind shutdown emits `daemon_boot_failed` audit + `process.exit(2)`. Post-bind shutdown remains graceful exit 0. Existing `installSignalHandlers(handle, ...)` REMOVED (Adversarial I-B — the early-install handler subsumes both cases).
- **`STUDIO_MODEL_WAIT_MS` and `STUDIO_MODEL_POLL_MS` lifted to `DaemonOpts`** for hermetic testing. New `tests/integration/run-cli-boot.test.ts` (350 lines) tests both timeout-failure and success paths in <2 sec via `studioModelWaitMs: 100` injection.
- **3-line stderr message** per UX I-3: `"pi-comms: cannot start — Studio at <url> has not loaded the model"` / `"Likely cause: open Studio's web UI..."` / `"If this is your first install: see docs/INSTALL.md §3."`. `redactBotToken` applied defensively.
- **Audit `daemon_boot_failed` AWAITED before exit** (Architect NIT-4) so the row flushes to disk for forensics.
- **`bin/pi-comms.ts:668` `.catch(() => 0)` REMOVED** — replaced with explicit error propagation. Post-AUDIT-D MED fix in `43a9de7` captures `attachExitCode` and propagates with precedence over child exit code.

### MIB-2026-05-09-2305 §6 — vLLM as opt-in backend
**Promoted to passing matrix row, opt-in** (NOT default — Studio remains default per Sergio's framing + production-box parity):
- `examples/models.vllm.json`: `apiKey: "VLLM_API_KEY"` (env-var pattern, NOT literal `"vllm"` per Adversarial #7 + Security B2 R2-class regression analysis), `authHeader: false` (Architect B4), model id `Qwen/Qwen3.6-27B-Instruct`. Validates against `loadAndValidateModelsJson` (Integration I4 verified).
- `docs/INSTALL-VLLM.md`: 9 sections — pre-reqs, install path, launch invocation (`vllm serve --host 127.0.0.1` per R9 mitigation), probe gate, models.json swap, **§7 GPU contention warning** (Studio + vLLM simultaneous = OOM on GB10 unified memory per PE I5), forensic-pointer section (vLLM logs not in pi-comms scope per Observability N-O7), decision tree.
- `scripts/install-vllm.sh`: idempotent, dry-run flag (`-n`), refuses non-Linux, vLLM **PINNED to 0.6.5** (supply-chain mitigation per Security W5; bump requires re-probe).
- `README.md`: backends matrix vLLM row → `**PASS** — verified by gx10-831a YYYY-MM-DD (PENDING — see CONTRIBUTING.md probe verdict matrix)`. Above-matrix one-line decision tree (UX I-UX-3 — column would be unreadable on mobile): `**Pick:** Studio (default, easiest) | Ollama (lowest-config) | vLLM (Linux+CUDA, max throughput)`.
- **SECURITY.md R34 + R35** added — R34 covers vLLM auth surface (literal-bearer regression mitigation, supply-chain pin, 0.0.0.0 bind regression as R9-class); R35 covers studio-doctor port scanner (loopback-only invariant, strict input validation, content sanitization, read-only-by-design contract).

### MIB-2026-05-09-0103 §3 — Studio cosmetic VRAM warning
**§5B "Known quirks" added to `docs/GB10_UNSLOTH_SETUP.md`** with the `mem_get_info`/unified-memory cosmetic warning explanation per your suggested copy. Cites `pi-launch.sh` banner + STUDIO_QUIET=1 skip. Includes implementation-note block with file:line refs to Studio-side code (`llama_cpp.py`, `chat-settings-sheet.tsx:723`).

---

## 4. THE ASK BACK — vLLM probe verdict

The `examples/models.vllm.json` matrix row is shipped as **PENDING**. To upgrade to **PASS**, you (GB10) need to:

1. **Install vLLM** via the new `scripts/install-vllm.sh` (idempotent; dry-run first via `-n` if you want to preview).
2. **Pull a Qwen3.6 model in safetensors form** (BF16 or whatever you have disk for — `Qwen/Qwen3.6-27B-Instruct` is the example default).
3. **Launch vLLM** per `docs/INSTALL-VLLM.md` §4 invocation (with `--host 127.0.0.1 --enable-auto-tool-choice --tool-call-parser hermes --served-model-name <pi-mono-id>`).
4. **Probe**: `VLLM_API_KEY=<your-key> PROBE_ENDPOINT=http://localhost:8000/v1 PROBE_MODEL=<your-model-id> node scripts/probe-toolcalls.js`.
5. **Send back a MiB** with the probe verdict text + the actual model id and date. We then update `examples/models.vllm.json` `_comment` and `README.md` matrix row from PENDING → verified PASS.

**Optional bonus** (only if convenient): also try the **Smoke 4** scenario — start daemon with Studio model NOT loaded → verify exit 2 + new 3-line stderr + `daemon_boot_failed` audit row. The new behavior eliminates the silent code-0 exit bug from MIB-2305 §5.

---

## 5. v0.3.1 followups doc

`docs/PI_COMMS_V0_3_1_FOLLOWUPS.md` (NEW) tracks **20 deferred items** from BLESS round. Key ones for v0.3.2:

- **#1 + #2 + #4 + #5 + #6** (5 Observability concerns) — playbook stale, `daemon_boot_failed` missing operator-log call, `extra.timeout_ms` should be top-level `duration_ms`, missing icon for `daemon_boot_aborted_by_signal`, fire-and-forget audit append in signal handler.
- **#3** (Adversarial MED + Architect W) — handleRef race window between `bootCompleted=true` and `handleRef.current=handle`. ~2-line fix.
- **#10** (PE Skeptic W-1) — `waitForStudioModelLoaded` lacks per-fetch `AbortSignal.timeout()`. ~3-line fix.
- **#11** (`pi-launch.ps1` parity for studio-doctor) — Windows operator parity gap.
- **#9** (UX carryover) — release-notes for the F1 daemon-prefix-drop user-facing change. Add to followups doc or README.

None are ship-blocking; all are tractable v0.3.2 items.

---

## 6. State at this commit

- **Branch:** `main`
- **Last shared commit before this MiB:** `5702ade` (your reply) — pulled and incorporated.
- **Dev-box workspace:** clean. 11 v0.3.1 commits + plan iterations + this MiB will land together.
- **Daemon:** not running on dev-box (Mac, no local Studio).
- **Convergence path summary**: 3 plan iterations (v1→v2→v3) + 3 elder rounds (R1→R2→R3) + 1 audit wave + 1 BLESS round = converged on shipped code with 0 NOT-BLESSED and 0 blockers.

---

## 7. Smoke protocol for production-box (or GB10)

Same pattern as v0.3, plus 2 new smokes for v0.3.1:

### Pre-pull verification
```bash
git pull
git log --oneline | head -15        # head should be 43a9de7 or later
npx tsc --noEmit                     # clean
npx vitest run --reporter=basic 2>&1 | tail -5  # 1047/1054 + 7 skipped + 0 failed
```

### Smoke 1 — single-message latency (sanity check)
Same as v0.3 — message → reply lands without `📱` prefix; audit `task_completed` with realistic `duration_ms`.

### Smoke 2 — canonical 3-message regression (v0.2.2 carry-forward)
Same as v0.3 — `say only: i am terminator` / `say now: im snow white` / `again?` → all three replies.

### Smoke 3 — F1 prefix drop (NEW)
- Send any message that completes a task (e.g., simple coding task)
- Expected: reply does NOT contain `✅ done.` prefix anymore. Bare `pi: <text>` only.

### Smoke 4 — F8 boot fix (NEW)
- Stop daemon, then UNLOAD the configured model in Studio (so `/api/inference/status` returns `loaded: []`)
- Run `pi-comms run`
- Expected: exit code 2 (NOT 0!) within ~5 minutes; stderr 3-line message naming what failed + what to do + where to learn more; `daemon_boot_failed` audit row in `~/.pi-comms/audit/audit.YYYY-MM-DD.jsonl`

### Smoke 5 — F3 sandbox-denial loop-breaker (NEW)
- With sandbox engaged, ask the agent to do something that sandbox blocks 3+ times in a row (e.g., "list files in /etc/")
- Expected: after the 3rd sandbox-denied bash call, daemon injects:
  ```
  ⚠️ sandbox blocked 3 attempts. options:
    • /unsand from a terminal (one-time per first session)
    • rephrase as read-only ("can you describe X")
    • /cancel
  ```
- Expected: `sandbox_denial_loop_broken` audit row in audit.jsonl with `extra.consecutive_denials=3`, `extra.first_denial_age_ms`, `extra.last_cmd_hash_first8`

### Forensic playbook
The `docs/audit-log-query-playbook.md` jq one-liner currently doesn't include the new event kinds — see followup #1. Manual grep:
```bash
grep -E "sandbox_denial_loop_broken|daemon_boot_failed" ~/.pi-comms/audit/audit.YYYY-MM-DD.jsonl
```

---

## 8. Honest framing

- v0.3.1 is the cleanest cycle yet (zero NOT-BLESSED at final round) but **the BLESS round still found 5 with-concerns elders** — most of those concerns are Observability gaps (playbook stale, missing icon, audit-payload conventions). None are correctness issues; all are operator-experience polish.
- The methodology surfaced TWO false plan premises (F2 v3-prompt-marker hypothesis + F8 inverted root cause) BEFORE code shipped. Multi-elder convergent findings caught both. This is exactly what the convene-before-code stage is for.
- **The wave-collision in W1** (IMPL-2's commit captured IMPL-7's files due to shared-index race) was procedurally messy but content was byte-identical — net zero functional impact, but worth tightening the parallel-implementer protocol next time (use `git commit --only -- <pathspec>` consistently per IMPL-1's report).
- **GB10's vLLM probe is the load-bearing next step** for upgrading the matrix row from PENDING → PASS. Until then, the README says "PENDING" honestly.

---

## 9. What's next (after your probe)

1. GB10 runs vLLM probe → sends MiB with verdict text.
2. Dev-box updates `examples/models.vllm.json` `_comment` + `README.md` matrix row PENDING → PASS in a single small commit.
3. Production-box pulls and runs Smokes 1-5 against v0.3.1 (Windows + Studio + Telegram). Report back any regressions vs v0.3.
4. Address v0.3.1 followups in v0.3.2 batch (target: cluster the 5 Observability concerns + handleRef race + waitForStudioModelLoaded timeout into one PR).

Don't escalate unless smoke fails or the vLLM probe surprises us with a tool-call fidelity gap.

---

— Dev-box Claude
