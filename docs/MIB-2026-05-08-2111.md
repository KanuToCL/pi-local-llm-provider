# Message in a Bottle — 2026-05-08 21:11 PT

> **From:** DGX Spark Claude (gx10-831a — NVIDIA GB10 Grace Blackwell)
> **To:** Dev-box Claude (orchestrator, wherever you are)
> **Re:** New machine commissioned. Bootstrap complete through PyTorch+CUDA. Ollama blocked on interactive sudo. pi-mono not yet installed. Repo not yet `npm install`'d. Several decisions need your input.
> **Token-budget note:** first-contact brief — hardware facts + blockers + open questions.

---

## 1. Machine identity

| Field | Value |
|---|---|
| Hostname | `gx10-831a` / `gx10-831a.local` |
| LAN IP | `192.168.86.37` |
| OS | Ubuntu (aarch64) — Linux 6.17.0-1014-nvidia |
| GPU | NVIDIA GB10 (Grace Blackwell Superchip) |
| NVIDIA driver | 580.142 |
| CUDA toolkit | 13.0 (nvcc), cu128 wheels working |
| SSH user | `kanuto` |
| GitHub | `KanuToCL` — SSH key provisioned, `ssh -T git@github.com` verified |

This is a DGX Spark. The GB10 is ARM64 + Blackwell GPU with unified memory. Memory pool shows `Not Supported` in nvidia-smi (expected on Grace Blackwell unified architecture).

---

## 2. What's provisioned so far

### Node.js (via nvm — user-scoped, no system packages)
```
nvm 0.40.3 → node v20.20.2 / npm 10.8.2
~/.nvm/ — default alias set to 20
```
Note: nvm is sourced via `~/.bashrc`. Interactive shells pick it up; non-interactive shells need `export NVM_DIR` + source before node is visible.

### Python venvs
```
~/.venvs/unsloth/
  torch 2.11.0+cu128  ← CUDA available: True, GPU: NVIDIA GB10
  unsloth 2026.5.2    ← dependency conflict: wants torch<2.11, we forced 2.11+cu128
  unsloth-zoo 2026.5.1
  transformers 5.5.0, peft, trl, accelerate, bitsandbytes...
```
**Known conflict:** `unsloth` pins `torch<2.11.0`. We forced `torch 2.11.0+cu128` after install to get CUDA. Everything imports; runtime behavior on GB10 needs smoke-testing with an actual model pull. If Unsloth breaks on 2.11, pin back to `torch==2.10.0+cu128` if that wheel exists for aarch64, or wait for Unsloth to support 2.11 officially.

`~/.venvs/vllm/` — **not yet created.** Deferred per user instruction (eventually).

### Repos cloned
```
~/Desktop/cosas/code/vibe_pdm/
~/Desktop/cosas/code/pi-local-llm-provider/
```

---

## 3. Blockers — needs interactive terminal or your input

### B1: Ollama not installed (needs sudo in interactive shell)
The Ollama install script requires `sudo` and an interactive terminal — cannot run via Claude Code's non-interactive bash. Sergio needs to run:
```bash
curl -fsSL https://ollama.com/install.sh | sh
```
Once installed, verify with `ollama --version` and `systemctl status ollama`.

### B2: pi-mono not installed
```bash
npm install -g @mariozechner/pi-coding-agent
```
Needs nvm sourced first. Sergio can run:
```bash
export NVM_DIR="$HOME/.nvm" && source "$NVM_DIR/nvm.sh" && npm install -g @mariozechner/pi-coding-agent
```

### B3: Repo not yet `npm install`'d
```bash
cd ~/Desktop/cosas/code/pi-local-llm-provider && npm install
```
Cannot run until B1/B2 are at least partially unblocked (npm is available, just needs nvm sourced).

### B4: `.env` not configured
`~/.pi/agent/models.json` doesn't exist yet — no provider config placed.
`.env` in repo not created — `TELEGRAM_BOT_TOKEN`, `UNSLOTH_API_KEY`, `PI_COMMS_DEFAULT_MODEL` all unset.

---

## 4. Open questions for dev-box

**Q1 — Unsloth Studio vs. Unsloth library:**
The `pi-local-llm-provider` README references Unsloth Studio at `http://localhost:8888` as a separate inference server distinct from the Python `unsloth` package (which is a fine-tuning library). What exactly is Unsloth Studio on this machine? Is it:
- A Docker container?
- The `unsloth` CLI with a `studio` subcommand?
- A separate binary/installer?

We installed the Python library. If Studio is a separate product, the venv may need to be rebuilt or supplemented.

**Q2 — Which model to start with on GB10:**
The repo's verified config used `unsloth/Qwen3.6-27B-GGUF` (Q4_K_M) on an RTX 5070 (VRAM-limited). GB10 has unified memory — the model can use system RAM as VRAM. What quantization and model should we target first for the smoke probe on this machine?

**Q3 — Probe target:**
Once Ollama is up, should we run the Track-1 probe (`node scripts/probe-toolcalls.js`) against Ollama first (simpler) or wait for Unsloth Studio to be sorted?

---

## 5. State at this MIB

- Branch: `main` (no local commits yet on this box)
- nvm + Node 20: ✅
- `~/.venvs/unsloth` + PyTorch CUDA: ✅ (torch version conflict noted)
- Ollama: ❌ blocked on interactive sudo
- pi-mono: ❌ not installed
- `npm install`: ❌ not run
- `.env` / `models.json`: ❌ not configured
- `~/.venvs/vllm`: deferred

**Items for dev-box (ranked):**
1. Clarify Unsloth Studio install path for aarch64 GB10 (Q1)
2. Recommend starting model + quantization for GB10 unified memory (Q2)
3. Confirm probe target order: Ollama first vs. Studio first (Q3)
4. Confirm whether torch 2.11.0+cu128 is acceptable or if we need to find a cu128 wheel for 2.10.0 on aarch64

---

— DGX Spark Claude (gx10-831a)
